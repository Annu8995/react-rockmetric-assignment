// console.log('utils.js is running!');

// const square = x => x*x;    
// const add = (a, b) => a+b;

// // export { square, add };   named exports

// // export const square = x => x*x; or  inline named exports like this
// // export const add = (a, b) => a+b;

// export { square, add as default};

// export default add;//or setting default like this

// export default (a, b) => a+b;